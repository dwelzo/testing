# py test
```
cd python

cd unit-testing

```
**then**

```
# Run tests
python -m pytest tests/test_main.py -v

# Run with coverage
python -m pytest tests/test_main.py --cov=app --cov-report=term-missing

# Try the demo
python demo.py

```